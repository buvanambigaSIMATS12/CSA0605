def find_max_min(arr):
    max_value = max(arr)
    min_value = min(arr)
    return min_value, max_value
N = 8
a = [5, 7, 3, 4, 9, 12, 6, 2]
min_value, max_value = find_max_min(a)
print(f"Min = {min_value}, Max = {max_value}")
